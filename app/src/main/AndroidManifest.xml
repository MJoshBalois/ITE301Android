<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.posapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Transcation"></activity>
        <activity android:name=".ViewMain" />
        <activity android:name=".SuperMain" />
        <activity android:name=".ProductEdit" />
        <activity android:name=".ProductView" />
        <activity android:name=".Product" />
        <activity android:name=".BrandEdit" />
        <activity android:name=".BrandView" />
        <activity android:name=".Brand" />
        <activity android:name=".CategoryEdit" />
        <activity android:name=".CategoryView" />
        <activity android:name=".Category" />
        <activity android:name=".Main" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>